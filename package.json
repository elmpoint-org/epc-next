{
  "scripts": {
    "dev": "concurrently \"npm:dev-*\"",
    "dev-types": "cd server && pnpm gql:gen --watch",
    "dev-client": "cd client && pnpm dev",
    "dev-server": "cd server && pnpm dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
