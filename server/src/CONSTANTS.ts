export const ALLOW_UNAUTH_USERS = true;

export const DEFAULT_KEY_LENGTH = 32;
export const JWT_ALGORITHM = 'HS256';

// prettier-ignore
export const LOGIN_EXPIRE = (24) * 3600; // (#) hours
// prettier-ignore
export const EMAIL_LOGIN_EXPIRE = (30) * 60 // (#) minutes
