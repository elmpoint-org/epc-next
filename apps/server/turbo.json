{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "gql:pregen": {
      "outputs": ["src/db/**/__types/**"]
    },
    "gql:gen": {
      "outputs": ["graphql-schema.d.ts"]
    }
  }
}
